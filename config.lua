-- your waffle's toppings A.K.A your config
-- TOPPINGS (your config) {
-- options for vim 
vo = {
  tabwidth=4,
  autoindent=true,
  colorscheme="default",
  cursorline=false,
  numbers=false,
  smartindent=true,
  swapfile=false,
  backup=false,
  smartcase=true,
  bells=false, -- error bells
  syntax='on', -- on or off
  leaderKey=';',
  
}
-- modes {
modes = {
   default = true,
   ide = false,
   tranquil = nil,
}

-- }
